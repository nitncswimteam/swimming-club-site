{
  "name": "swimming_club_site",
  "version": "1.0.0",
  "description": "奈良高専水泳部の公式サイトです！",
  "author": "chige12",
  "private": true,
  "scripts": {
    "dev": "rm -r contents/_output_file & processmd contents/news/**/*.md --stdout --outputDir contents/_output_file/news --html true > contents/summary_news.json && processmd contents/results/**/*.md --stdout --outputDir contents/_output_file/results --html true > contents/summary_results.json && nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "rm -r contents/_output_file & processmd contents/news/**/*.md --stdout --outputDir contents/_output_file/news > contents/summary_news.json && processmd contents/results/**/*.md --stdout --outputDir contents/_output_file/results > contents/summary_results.json && nuxt generate"
  },
  "dependencies": {
    "@toast-ui/vue-editor": "^1.1.1",
    "codemirror": "^5.48.2",
    "cross-env": "^5.2.0",
    "nuxt": "^2.8.1",
    "processmd": "nitncswimteam/processmd",
    "tui-editor": "^1.4.6",
    "vue-lazyload": "^1.3.2",
    "vue-magic-grid": "^0.0.4"
  },
  "devDependencies": {
    "mathsass": "^0.10.1",
    "node-sass": "^4.12.0",
    "nodemon": "^1.19.1",
    "pug": "2.0.4",
    "pug-plain-loader": "^1.0.0",
    "sass-loader": "^7.2.0"
  }
}
